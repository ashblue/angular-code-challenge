'use strict';

/**
 * @ngdoc function
 * @name interviewApp.controller:ContactCtrl
 * @description
 * # ContactCtrl
 * Controller of the interviewApp
 */
angular.module('interviewApp')
  .controller('ContactCtrl', function () {
    // Optional controller (currently not used)
  });
